namespace Unity.tag
{
    public enum ButtonTag { None, Left, Right, Jump };
    public enum JoystickTag { None, Weapon };
    public enum InputMode { Touch, Key };
    public enum BodyTab { None, Head, Arm, Leg};
}
